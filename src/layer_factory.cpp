//
// Created by huangshize on 16-4-25.
//

#include "layer_factory.h"

namespace jaffe {
	/*
    template <typename Dtype>
    shared_ptr<JLayer<Dtype> > GetConvolutionLayer(const LayerParameter& param){
        // Unfinished
    }
    REGISTER_LAYER_CREATOR(Convolution, GetConvolutionLayer);

    template <typename Dtype>
    shared_ptr<JLayer<Dtype> > GetPoolingLayer(const LayerParameter& param) {
        // Unfinished
    }
    REGISTER_LAYER_CREATOR(Pooling, GetPoolingLayer);

    template <typename Dtype>
    shared_ptr<JLayer<Dtype> > GetLRNLayer(const LayerParameter& param) {
        // Unfinished
    }
    REGISTER_LAYER_CREATOR(LRN, GetLRNLayer);

    template <typename Dtype>
    shared_ptr<JLayer<Dtype> > GetReLULayer(const LayerParameter& param) {
        // Unfinished
    }
    REGISTER_LAYER_CREATOR(ReLU, GetReLULayer);

    template <typename Dtype>
    shared_ptr<JLayer<Dtype> > GetSigmoidLayer(const LayerParameter& param) {
        // Unfinished
    }
    REGISTER_LAYER_CREATOR(Sigmoid, GetSigmoidLayer);

    template <typename Dtype>
    shared_ptr<JLayer<Dtype> > GetSoftmaxLayer(const LayerParameter& param) {
        // Unfinished
    }
    REGISTER_LAYER_CREATOR(Softmax, GetSoftmaxLayer);
	*/
} // namespace jaffe
